server:
  port: 8089
spring:
  application:
    name: hmdp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/hmdp?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: 20000328
  redis:
    host: 127.0.0.1
    port: 6379
    password: ""
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null # JSON处理时忽略非空字段
mybatis-plus:
  type-aliases-package: com.hmdp.entity # 别名扫描包
logging:
  level:
    com.hmdp: debug

ai:
  # 兼容 OpenAI 的 Chat Completions 接口（百炼内地站）
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-flash
  embedding-model: text-embedding-v1
  embedding-dimensions: 1024
  embedding-encoding-format: float
  api-key: sk-a5dff1f83c314502bd6561924e0e2356
  temperature: 0.2
  max-tokens: 512
  snippet-max-chars: 300
  top-k: 10
  context-max-chars: 4000
  cache-ttl-seconds: 300
